# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.tsbuildinfo
.playwright-cli
.playwright-mcp/
android-test-*.png
*.local
.env
.env.*
!.env.example
*.sqlite
*.sqlite3
*.db

# Editor directories and files
.vscode/
.idea
.workbuddy/
.agents/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only build artifacts and process snapshots (never commit)
dist-*/
docs/
AGENTS.md
CLAUDE.md
RULE.md
.tmp/
output/
codex-notes/

# Local-only UI mockup (high-fidelity static render, never commit)
设计稿/

# Tauri generated mobile project (init/build artifacts, contains 2.5GB gradle+APK)
src-tauri/gen/

# Android release signing keystore (never commit credentials)
src-tauri/*.keystore
