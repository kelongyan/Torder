{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Capability for the desktop widget window",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["widget"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
