{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mini",
  "description": "Capability for the mini quick-add window",
  "platforms": ["windows", "macOS", "linux"],
  "windows": ["mini"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
